default:
  interruptible: true

  # From this line onwards the properties are shared with ./crate-publishing-pipeline.yml
  # TODO: find way to avoid manual repetition
  retry:
    max: 2
    when:
      - runner_system_failure
      - unknown_failure
      - api_failure
  cache: {}
