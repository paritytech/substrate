[package]
name = "sp-consensus"
version = "2.0.0"
authors = ["Parity Technologies <admin@parity.io>"]
description = "Common utilities for substrate consensus"
edition = "2018"

[dependencies]
derive_more = "0.99.2"
libp2p = { git = "https://github.com/expenses/rust-libp2p", branch = "rand-wasm", default-features = false }
log = "0.4.8"
primitives = { package = "sp-core", path= "../../core" }
inherents = { package = "sp-inherents", path = "../../inherents" }
futures = { version = "0.3.1", features = ["thread-pool"] }
wasm-timer = "0.2.0"
sp-std = { path = "../../std" }
runtime_version = { package = "sp-version", path = "../../sr-version" }
sp-runtime = {  path = "../../runtime" }
codec = { package = "parity-scale-codec", version = "1.0.0", features = ["derive"] }
parking_lot = "0.9.0"

[dev-dependencies]
test-client = { package = "substrate-test-runtime-client", path = "../../../test/utils/runtime/client" }

[features]
default = []
