[package]
name = "substrate-test-runtime-wasm"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]
edition = "2018"

[lib]
name = "substrate_test_runtime"
crate-type = ["cdylib"]

[dependencies]
substrate-test-runtime = { path = "..", default-features = false }

[features]
default = [
]
std = [
	"substrate-test-runtime/std",
]

[profile.release]
panic = "abort"
lto = true

[workspace]
members = []

[patch.crates-io]
trie-db = { path = "../../../../parity-trie/trie-db" }
hash-db = { path = "../../../../parity-trie/hash-db" }
memory-db = { path = "../../../../parity-trie/memory-db" }
elastic-array = { path = "../../../../elastic-array" }
