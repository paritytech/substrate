[package]
name = "wasm-builder"
version = "2.0.0"
authors = ["Parity Technologies <admin@parity.io>"]
description = "Utility for building WASM binaries"
edition = "2018"

[dependencies]
build-helper = "0.1.1"
cargo_metadata = "0.8"
tempfile = "3.0.8"
toml = "0.5.1"
