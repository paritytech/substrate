[package]
name = "node-executor"
version = "0.1.0"
authors = ["Parity Technologies <admin@parity.io>"]
description = "Substrate node implementation in Rust."

[dependencies]
hex-literal = "0.1"
triehash = "0.2"
parity-codec = { path = "../../codec" }
sr-io = { path = "../../core/sr-io" }
substrate-state-machine = { path = "../../core/state-machine" }
substrate-executor = { path = "../../core/executor" }
substrate-primitives = { path = "../../core/primitives" }
node-primitives = { path = "../primitives" }
node-runtime = { path = "../runtime" }

[dev-dependencies]
substrate-keyring = { path = "../../core/keyring" }
sr-primitives = { path = "../../core/sr-primitives" }
substrate-runtime-support = { path = "../../core/srml/support" }
substrate-runtime-balances = { path = "../../core/srml/balances" }
substrate-runtime-session = { path = "../../core/srml/session" }
substrate-runtime-staking = { path = "../../core/srml/staking" }
substrate-runtime-system = { path = "../../core/srml/system" }
substrate-runtime-consensus = { path = "../../core/srml/consensus" }
substrate-runtime-timestamp = { path = "../../core/srml/timestamp" }
substrate-runtime-treasury = { path = "../../core/srml/treasury" }
