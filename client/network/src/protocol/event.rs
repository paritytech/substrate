// Copyright 2019 Parity Technologies (UK) Ltd.
// This file is part of Substrate.

// Substrate is free software: you can redistribute it and/or modify
// it under the terms of the GNU General Public License as published by
// the Free Software Foundation, either version 3 of the License, or
// (at your option) any later version.

// Substrate is distributed in the hope that it will be useful,
// but WITHOUT ANY WARRANTY; without even the implied warranty of
// MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
// GNU General Public License for more details.

// You should have received a copy of the GNU General Public License
// along with Substrate.  If not, see <http://www.gnu.org/licenses/>.

//! Network event types. These are are not the part of the protocol, but rather
//! events that happen on the network like DHT get/put results received.

use libp2p::core::PeerId;
use libp2p::kad::record::Key;
use std::borrow::Cow;

/// Events generated by DHT as a response to get_value and put_value requests.
#[derive(Debug, Clone)]
pub enum DhtEvent {
	/// The value was found.
	ValueFound(Vec<(Key, Vec<u8>)>),

	/// The requested record has not been found in the DHT.
	ValueNotFound(Key),

	/// The record has been successfully inserted into the DHT.
	ValuePut(Key),

	/// An error has occured while putting a record into the DHT.
	ValuePutFailed(Key),
}

/// Type for events generated by networking layer.
#[derive(Debug, Clone)]
pub enum Event {
	/// Opened a substream with the given node with the given notifications protocol.
	///
	/// The protocol name is always one of the notification protocols that have been registered
	/// in the configuration.
	NotifOpened {
		/// Node we opened the substream with.
		remote: PeerId,
		/// Name of the concerned protocol. Each protocol uses a different substream.
		proto_name: Cow<'static, [u8]>
	},

	/// Closed a substream with the given node. Always matches a corresponding previous
	/// `NotifOpened` message.
	NotifClosed {
		/// Node we closed the substream with.
		remote: PeerId,
		/// Name of the concerned protocol. Each protocol uses a different substream.
		proto_name: Cow<'static, [u8]>,
	},

	/// Received a message from the given node using the given protocol.
	NotifMessages {
		/// Node we received the message from.
		remote: PeerId,
		/// Concerned protocol and associated message.
		messages: Vec<(Cow<'static, [u8]>, Vec<u8>)>,
	},

	/// Event generated by a DHT.
	Dht(DhtEvent),
}
